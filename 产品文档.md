# 产品文档：koocuu-weixin-mcp

## 要解决的问题

让 AI 客户端（Claude / Codex / ChatGPT）通过 Remote MCP 管理微信公众号：写稿、上传素材、草稿、菜单、二维码，以及受控发布。需要稳定公网入口与固定出口 IP，以满足微信 API IP 白名单。

## 目标用户

公众号运营者本人（单账号、自用）。不是多租户 SaaS。

## 核心场景

1. 在 Claude / Codex 中连接 `https://weixin.koocuu.com/api/mcp`，完成一次 OAuth 授权（授权页粘贴 `MCP_BEARER_TOKEN`）。
2. 用 MCP 工具生成文章 HTML、上传封面/正文图、创建与更新草稿。
3. 在确认后发布草稿（默认关闭，需显式开启并二次确认）。
4. 接收微信服务器回调校验与可选明文自动回复。

## 功能范围

- Markdown/HTML → 微信图文 HTML
- 临时/永久图片素材上传
- 草稿 CRUD、菜单读写、二维码 ticket
- 出站 IP 探测（便于白名单排障）
- 受控 freepublish（`WECHAT_ENABLE_PUBLISH` + `confirm` + `dryRun`）
- OAuth（MCP 客户端拿 bearer，非多用户账号体系）

## 非目标

- 群发、多公众号租户、复杂权限体系
- 依赖本机长期开机的生产方案（本地 Tunnel 仅作开发/过渡）

## 架构流程

```text
Claude / Codex / ChatGPT
        │
weixin.koocuu.com（Cloudflare Worker 反代）
        │
腾讯云 SCF 广州区 ap-guangzhou（Next.js Docker，端口 9000）
        │
固定公网出口 IP → 微信公众号 API
        │
Upstash Redis（OAuth client、微信 token、任务状态；生产强烈建议）
```

说明：个人版 TCR 镜像区域不含香港，故 SCF 使用广州；公网入口仍在 Cloudflare，无需 ICP。

## 当前部署状态（摘要）

- 入口：`https://weixin.koocuu.com`（health 已通）
- MCP：`https://weixin.koocuu.com/api/mcp`
- SCF Function URL：`https://1302249545-lgmt20iu8p.ap-guangzhou.tencentscf.com`
- 固定出口 IP：`129.204.3.249`（需写入微信 API IP 白名单）
- Claude.ai Connect：**尚未验收通过**（详见 `进度.md`）

## 验收标准

1. `https://weixin.koocuu.com/api/health` 返回正常。
2. Claude 自定义连接器可用 MCP 端点完成 OAuth 并调用工具。
3. SCF 固定出口 IP 已写入微信 API IP 白名单，微信接口不再因 IP 报错。
4. 微信回调 URL 仍为 `https://weixin.koocuu.com/api/wechat/callback` 且校验通过。
5. 发布类工具在默认配置下不可执行；开启后仍需 `confirm` + `dryRun:false`。
