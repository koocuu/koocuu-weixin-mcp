# 进度

## 当前现状（2026-07-11）

### 混合架构（代码已落地，待部署）

```text
Claude / Cursor
    │
    ▼
Vercel（MCP + OAuth）  https://koocuu-weixin.vercel.app
    │  WECHAT_RELAY_URL
    ▼
POST /api/wechat-relay   ← 鉴权 Bearer WECHAT_RELAY_SECRET
    │
    ▼
SCF 广州（固定出口 129.204.3.249）
    │
    ▼
api.weixin.qq.com

微信服务器回调仍可：
微信 → weixin.koocuu.com（CF Worker）→ SCF /api/wechat/callback
```

### 代码改动

- `app/api/wechat-relay/route.ts` + `src/wechat/relay-*.ts`：SCF 中继（http / upload / outbound_ip）
- `src/wechat/client.ts`：存在 `WECHAT_RELAY_URL` 时全部微信流量走中继
- `wechat_get_outbound_ip`：混合模式下返回 SCF 出口 IP
- SCF workflow：注入 `WECHAT_RELAY_SECRET`（= `MCP_BEARER_TOKEN`）；**不要**在 SCF 设 `WECHAT_RELAY_URL`

### 部署清单（待执行）

1. **部署 SCF 新镜像**（含 `/api/wechat-relay`）：跑 GitHub Action `Deploy Tencent SCF`
2. **Vercel 环境变量**（Production）：
   - `PUBLIC_BASE_URL=https://koocuu-weixin.vercel.app`
   - `WECHAT_RELAY_URL=https://weixin.koocuu.com`（或 SCF Function URL）
   - `WECHAT_RELAY_SECRET=<与 MCP_BEARER_TOKEN 相同>`
   - 其余微信 / MCP 密钥保持不变
3. **Redeploy Vercel**
4. Claude 连接器改用：`https://koocuu-weixin.vercel.app/api/mcp`
5. 验证：`wechat_get_outbound_ip` 应返回 `129.204.3.249`
6. CF Worker：继续服务 `weixin.koocuu.com`（回调 + 中继入口）；Claude 不再依赖它

### 判决结论（保留）

| 客户端 | Vercel | SCF/CF 直连 MCP |
| --- | --- | --- |
| Claude.ai | 能连 | 不能（超时/SSE） |
| Codex / 本机 | 能连 | 能连 |

SCF 保留价值：微信 API **静态出口 IP**，不是 Claude 入口。
