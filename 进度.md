# 进度

## 当前现状（2026-07-10 晚）

### 架构（已落地）

```text
Claude / 微信回调
        │
https://weixin.koocuu.com
        │  Cloudflare Worker（账号 cuyooh@gmail.com）
        │  名称：koocuu-weixin-mcp-proxy
        ▼
腾讯云 SCF 广州区（ap-guangzhou）
Function URL：https://1302249545-lgmt20iu8p.ap-guangzhou.tencentscf.com
固定出口 IP：129.204.3.249
        ▼
微信公众号 API
```

### 已验证通过

| 检查项 | 结果 |
| --- | --- |
| SCF `/api/health` 直连 | 200 |
| `https://weixin.koocuu.com/api/health` | 200 |
| DNS `weixin` → Worker `koocuu-weixin-mcp-proxy` | 已代理 |
| 本机模拟 OAuth：register → authorize → token | 成功 |
| 本机 `POST /api/mcp` initialize + Bearer | 200 |

### 未打通

- **Claude.ai 自定义连接器 OAuth 仍失败**
  - 参考号：`ofid_f80212b511ca014c`、`ofid_df0ace12fe9fc263`
  - 现象：授权页可打开并提交 `MCP_BEARER_TOKEN` 后，Claude 报 `Authorization with weixin failed`
  - 本机端到端 OAuth/MCP 探测正常，更像 Claude 后端回调/校验与边缘策略或 AS 元数据细节问题

### 已做修复（代码已入库，部分需重部署）

1. **SCF 区域**：个人版 TCR 不能给香港 SCF 用 → 改为 `ap-guangzhou`；workflow 去掉非法 `CodeSource=Image`
2. **Cloudflare 账号**：错账号作废；正确账号 `cuyooh@gmail.com` 上手动建 Worker 并绑域名
3. **Worker 剥 `Content-Disposition: attachment`**：SCF Function URL 会注入该头，导致浏览器下载 `authorize.htm`
4. **Worker / 应用补 RFC 9207 `iss`**：授权 302 Location 增加 `iss=https://weixin.koocuu.com`
   - 应用层：`src/auth/oauth.ts`（**需重新构建并部署 SCF 镜像后才在源站生效**）
   - Worker 层：`cloudflare-worker/src/index.js`（控制台 Deploy 即可立刻生效）

### 云端资源速查

| 项 | 值 |
| --- | --- |
| 腾讯云 UIN | `100014381511` |
| APPID | `1302249545` |
| TCR | `ccr.ccs.tencentyun.com/koocuu/koocuu-weixin-mcp` |
| SCF 函数 | `koocuu-weixin-mcp` / namespace `default` / `ap-guangzhou` |
| CF 域名账号 | `cuyooh@gmail.com` |
| Worker | `koocuu-weixin-mcp-proxy` |
| 公网入口 | `https://weixin.koocuu.com` |
| MCP | `https://weixin.koocuu.com/api/mcp` |
| 微信回调 | `https://weixin.koocuu.com/api/wechat/callback` |

### 待办（交接）

1. **Claude Connect**：继续排障（建议先确认 Worker 已 Deploy 含 `iss` + 剥 attachment 的版本；再在 CF 自定义规则放行 Anthropic 出口 `160.79.104.0/21`；必要时向 Anthropic 提供 ofid）
2. **微信 API IP 白名单**加入：`129.204.3.249`
3. **补 Upstash Redis** 到 SCF / GitHub `Production`（`UPSTASH_REDIS_REST_*` 目前为空；OAuth client 注册依赖 store，多实例下建议必配）
4. **GitHub `Production` Cloudflare secrets** 换成正确账号（`cuyooh@gmail.com`）的 Token / Account ID；错账号 secrets 作废
5. **触发 `Deploy Tencent SCF`**，让源站也带上 `iss` 等应用层改动
6. Claude 通后再考虑停用旧 Tunnel / Vercel 过渡部署

### 本次提交影响文件

- `.github/workflows/deploy-scf.yml`：广州区、去掉 `CodeSource=Image`
- `docs/tencent-scf.md`：区域与入口说明
- `cloudflare-worker/*`：实写 `SCF_ORIGIN`；剥 attachment；302 补 `iss`
- `src/auth/oauth.ts` + 测试：授权回调带 `iss`
- `产品文档.md`、`进度.md`

### 验证说明

- 已跑：`vitest` OAuth 相关测试通过；公网 health / 本机 OAuth·MCP 探测通过
- 未通：Claude.ai Connect（见 ofid）
- 未在本机执行：SCF 镜像重建推送（需 GitHub Actions 或本机 Docker + TCR）

---

## 历史记录

### 2026-07-10：SCF 落地与 Cloudflare 切换

接续 Codex `c531552`。Actions 首次因 `CodeSource=Image` 失败；SDK 创建时发现个人版 TCR 不含香港 → 改广州。公网仍走 Cloudflare，无需 ICP。

### 2026-07-10：正确 CF 账号绑域名

错账号无 `koocuu.com`；`cuyooh@gmail.com` 控制台建 Worker + DNS Worker 记录。`wrangler login` OAuth 本机回调失败，未用 CLI 部署。
